#cloud-config
package_update: true
package_upgrade: true
packages:
  - git
  - jq
  - curl
  - unzip
  - python3
  - python3-venv
  - python3-pip

runcmd:
  - mkdir -p /opt/aurora
  - chmod 755 /opt/aurora

